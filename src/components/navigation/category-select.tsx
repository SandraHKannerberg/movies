"use client";

import React, { use, useCallback } from "react";
import { Genre } from "@/lib/interfaces/category-interfaces";

export const CategorySelect = ({
  categories,
}: {
  categories: Promise<Genre[]>;
}) => {
  return <div>Filter goes here</div>;
};
